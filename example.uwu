package main;

impowt io::pwintwn;
impowt io::wwite;
impowt mem::gc;

stwuct Bwah {
    content: i64[],
    wepeats: i64
}

fuwn test() {
    wet test = new Bwah("testing", 10);
    pwintwn(test.content);
    gc();

    test.content = "hewwo, wowwd! ~~";
    pwintwn(test.content);

    whiwe (test.wepeats > 0) {
        wwite('a' + test.wepeats);
        test.wepeats = test.wepeats - 1;
    }
    pwintwn("");
}

fuwn main() {
    test();
    gc();
    gc();
}
